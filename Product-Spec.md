# Product Spec：微信抢单系统

## 产品说明

这是一套监控微信群中家教/辅导接单消息并自动回复抢单的系统。用户只需在Windows电脑上登录微信，系统后台持续监听指定群聊消息，通过智能匹配识别接单信息后自动引用回复指定内容（默认“1”）。同时支持本地配置界面与微信指令两种方式切换模式、筛选类型，并记录抢单历史供用户查询。

**一句话**：你开了电脑挂着微信，群里一发接单信息，系统秒回——再也不用手速拼不过别人。

**核心理念**：不碰微信协议、不破解客户端、纯模拟人操作，封号风险最低。

**产品路线**：第一阶段做自用版跑通流程；第二阶段升级为商业版，支持多用户、授权管理、订阅收费。

---

## 功能需求

### 第一阶段·自用版

- **群消息监听**：监听指定群的文字消息（单个群或多个群），实时捕获
- **关键词快速匹配**：消息中出现“词汇/阅读/语法/完型/听口/写作/抗遗忘”之一，毫秒级响应
- **AI智能匹配**：无关键词但消息内容被AI判定为家教/辅导/培训类接单信息，同样响应
- **无类型默认词汇**：AI判定为接单但未提取到具体类型时，自动默认类型为“词汇”
- **引用回复**：自动引用原消息回复指定内容，默认“1”，支持自定义
- **单项模式**：用户勾选一种类型，只有消息中的类型匹配才抢
- **多项模式**：AI判断为接单信息即抢，不限类型
- **配置界面**：本地Web页面，勾选模式、选择类型、指定群聊、自定义回复内容
- **微信指令控制**：向机器人发送“切单项”“切多项”“暂停”“恢复”“状态”等指令
- **抢单记录**：后端记录每次抢单的时间、群名、消息原文、抢单结果、匹配类型
- **实时提醒**：抢单成功后，在系统托盘弹出通知

### 课表管理（新增）

- **每周课表视图**：以周日历方式展示已抢订单排课情况，一目了然看出周一到周日哪些时间被占
- **抢单自动入课表**：AI从消息中提取上课时间（周几、几点、时长），抢单成功后自动添加到课表
- **手动添加课表**：支持在配置页面手动添加/编辑/删除课表条目
- **冲突检测**：新增课表（抢单或手动）时，自动检测时间是否与已有课程冲突，弹窗提醒
- **今日课表提醒**：每天早上或电脑开机时，弹出今日上课安排提醒
- **标注状态**：每节课可标注为“待确认”“已确认”“已取消”“已完成”
- **学生备注**：每个课表条目可添加学生信息、备注（如“基础中等，需耐心”）
- **导出课表**：支持复制或导出为文本格式的周课表，方便分享给他人

### 第二阶段·商业版（新增）

- **一键安装包**：打包为安装程序（Inno Setup），用户双击安装，一路下一步
- **授权激活**：启动时绑定当前登录的微信号，校验授权状态
- **授权管理**：在线验证授权码，未授权无法使用核心功能
- **自动更新**：检测新版本并提示升级
- **远程售后辅助**：授权过期或出问题时，提供远程查看日志功能
- **多开支持**：同一台电脑可同时监听多个微信号

---

## UI 布局

### 整体架构

配置部分为**本地网页**（浏览器打开），运行部分为**后台脚本**（无界面）。

### UI 布局

### 整体架构

配置部分为**本地网页**（浏览器打开），运行部分为**后台脚本**（无界面）。

### 配置页面（浏览器打开 http://localhost:4876）

`
┌──────────────────────────────────────────────────┐
│  🎯 微信抢单系统 · 配置面板                       │
├──────────────────────────────────────────────────┤
│  [状态指示器]  ● 运行中 ｜ 暂停 ｜ 已停止          │
├──────────────────────────────────────────────────┤
│  ┌─ 基础设置 ─────────────────────────────┐      │
│  │  ☑ 开机自启动                           │      │
│  │  监听模式：[单个群 ▼] ｜ [多个群 ▼]     │      │
│  │  ┌─ 选择群聊 ──────────────────┐       │      │
│  │  │ ☑ 接单群①                    │       │      │
│  │  │ ☑ 英语教练群                 │       │      │
│  │  │ ☐ 同学闲聊群                 │       │      │
│  │  │ [+ 刷新群列表]               │       │      │
│  │  └──────────────────────────────┘       │      │
│  └──────────────────────────────────────────┘      │
│                                                     │
│  ┌─ 课表管理 ─────────────────────────────┐      │
│  │  ┌─ 周课表 ─────────────────────┐      │      │
│  │  │ 时间  │ 周一 │ 周二 │ ...  │      │      │
│  │  │ 08:00 │      │      │       │      │      │
│  │  │ 09:00 │      │ 张三 │       │      │      │
│  │  │ 10:00 │ 李四 │      │       │      │      │
│  │  │  ...  │      │      │       │      │      │
│  │  └──────────────────────────────┘       │      │
│  │  [添加上课] [导出课表] [刷新]           │      │
│  │                                           │      │
│  │  ☑ 抢单后自动加入课表                    │      │
│  │  ☑ 冲突时提醒（不阻止抢单）              │      │
│  │  ☑ 开机时显示今日课表                    │      │
│  └──────────────────────────────────────────┘      │
│                                                     │
│  ┌─ 抢单模式 ─────────────────────────────┐      │
│  │  ○ 多项模式（见单就抢）                  │      │
│  │  ● 单项模式（只抢勾选类型）              │      │
│  │                                           │      │
│  │  勾选要抢的类型：                         │      │
│  │  ☑ 词汇  ☑ 阅读  ☐ 语法                │      │
│  │  ☑ 完型  ☐ 听口  ☑ 写作  ☑ 抗遗忘         │      │
│  └──────────────────────────────────────────┘      │
│                                                     │
│  ┌─ 回复设置 ─────────────────────────────┐      │
│  │  回复内容：[    1    ] （默认“1”）       │      │
│  │  📌 支持任意文字，如“已接”“我来”“111”   │      │
│  └──────────────────────────────────────────┘      │
│                                                     │
│  ┌─ 智能匹配 ─────────────────────────────┐      │
│  │  ● 启用AI智能识别（识别无关键词的接单）  │      │
│  │  ● 无类型时默认：词汇                   │      │
│  │  AI接口：[▼ OpenAI ]                     │      │
│  │  API Key：••••••••••••••••••            │      │
│  │  [测试连接]                              │      │
│  └──────────────────────────────────────────┘      │
│                                                     │
│  ┌─ 抢单记录 ─────────────────────────────┐      │
│  │  2026-07-05 14:23:15 ｜ 接单群①        │      │
│  │  │  正课教练 高中 男生 ...  [词汇+阅读] │      │
│  │  │  ✅ 已抢                             │      │
│  │  ─────────────────────────────────      │      │
│  │  2026-07-05 13:10:42 ｜ 英语教练群      │      │
│  │  │  准高一 女 需有耐心...   [默认词汇]  │      │
│  │  │  ✅ 已抢                             │      │
│  │  [查看全部记录 →]                       │      │
│  └──────────────────────────────────────────┘      │
└──────────────────────────────────────────────────┘
`

### 课表页面（独立标签页）

`
┌──────────────────────────────────────────────────┐
│  课表管理 ｜ 本周 2026-07-06 ~ 2026-07-12        │
│  [← 上一周] [📅 回到今天] [下一周 →]             │
├──────────────────────────────────────────────────┤
│  ┌────────┬──────┬──────┬──────┬──────┬──────┐  │
│  │ 时间   │ 周一 │ 周二 │ 周三 │ 周四 │ 周五 │  │
│  ├────────┼──────┼──────┼──────┼──────┼──────┤  │
│  │ 08:00  │      │      │      │      │      │  │
│  │ 09:00  │      │ 张三 │      │      │      │  │
│  │        │      │ 词汇 │      │      │      │  │
│  ├────────┼──────┼──────┼──────┼──────┼──────┤  │
│  │ 20:00  │      │      │ 李四 │      │      │  │
│  │        │      │      │ 阅读 │      │      │  │
│  │        │      │      │ ✅已确认│      │      │  │
│  ├────────┼──────┼──────┼──────┼──────┼──────┤  │
│  │ 20:30  │ 王五 │      │      │      │ 赵六 │  │
│  │        │ 语法 │      │      │      │ 完型 │  │
│  │        │ ⏳待确认 │      │      │      │ ✅已确认│  │
│  └────────┴──────┴──────┴──────┴──────┴──────┘  │
│                                                     │
│  ┌─课程总数: 4 节 ｜ 本周收入预估: -- 元───────┐  │
│  └──────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────┤
│  ▼ 课表明细列表（按时间倒序）                      │
│  ┌──────────────────────────────────────────┐     │
│  │ 周三 20:00-21:00 李四 阅读 ✅已确认       │     │
│  │   学生：新初一男生 基础中等 需有耐心        │     │
│  │   [编辑] [取消] [标记完成]                 │     │
│  ├──────────────────────────────────────────┤     │
│  │ 周一 20:30-21:30 王五 语法 ⏳待确认        │     │
│  │   学生：高中男生 4.6级                     │     │
│  │   来源：接单群① 2026-07-05 抢单           │     │
│  │   [编辑] [取消] [标记完成]                 │     │
│  └──────────────────────────────────────────┘     │
└──────────────────────────────────────────────────┘
`

### 系统托盘图标（Windows 右下角）

- 右键菜单：打开配置 / 暂停抢单 / 恢复抢单 / 激活授权 / 关于 / 退出
- 抢单成功弹出气泡通知

---

## 用户使用流程

### 首次使用（自用版）

1. 下载并解压程序包
2. 双击运行，系统托盘出现图标
3. 浏览器自动打开配置页面（或手动打开 http://localhost:4876）
4. 配置：
   - 点击“刷新群列表”，勾选要监听的群
   - 选择“多项模式”或“单项模式”，单项则勾选类型
   - 回复内容：默认“1”，可改为任意文字
   - （可选）填写AI API Key，启用AI智能识别
   - 点击“保存”
5. 确保微信电脑版已登录，且在群列表中能看到目标群
6. 系统开始运行

### 首次使用（商业版）

1. 下载安装包，双击安装（Inno Setup 安装向导）
2. 安装完成自动启动
3. 微信扫码登录（弹窗提示）
4. 浏览器打开配置页面 → 提示“未激活”
5. 输入授权码，点击激活
6. 激活成功，进入配置页面
7. 配置完成，系统开始运行

### 日常使用

1. 电脑挂机，微信保持登录
2. 群里一发接单消息 → 系统自动引用回复“1”（或自定义内容）
3. 系统托盘弹出通知“✅ 已抢单：xx群”
4. （如果启用课表自动添加）AI提取时间信息 → 自动排入课表 → 在配置页面可见
5. 每天早上弹出“📋 今日课表”通知，提醒今日上课时间
6. 如需切换模式 → 发微信消息“切单项阅读” 或 打开网页修改

### 特殊场景

- **暂停/恢复**：发微信消息“暂停”或右键托盘菜单暂停，恢复同理
- **查看状态**：发微信消息“状态”，机器人回复当前模式和配置
- **电脑重启**：如果配置了开机自启，系统自动启动并开始监听

---

## 补充说明

### 配置项

| 配置项 | 可选值 | 默认值 | 说明 |
|-------|-------|-------|------|
| 监听模式 | 单个群 / 多个群 | 多个群 | 单个群时自动选中已勾选的第一个群 |
| 抢单模式 | 单项 / 多项 | 多项 | 多项见单就抢，单项需勾选类型 |
| 可选类型 | 词汇、阅读、语法、完型、听口、写作、抗遗忘 | 全部勾选 | 仅在单项模式下生效 |
| 回复内容 | 任意文本 | “1” | 自定义回复时引用的内容 |
| AI智能识别 | 启用 / 关闭 | 关闭 | 启用需填写API Key |
| 无类型默认 | 词汇 / 不处理 | 词汇 | AI判定为接单但未提取类型时的兜底行为 |
| AI接口 | OpenAI / 其他兼容API | OpenAI | 调用LLM做消息智能判断 |
| 开机自启 | 启用 / 关闭 | 关闭 | 写入Windows启动项 |
| 回复延迟 | 0-5秒 | 0 | 防止机器人特征过于明显 |

### 课表配置项（新增）

| 配置项 | 可选值 | 默认值 | 所属功能 | 说明 |
|-------|-------|-------|--------|------|
| 自动入课表 | 启用 / 关闭 | 关闭 | 基础 | 抢单成功后AI提取时间自动加入课表 |
| 冲突检测 | 启用 / 关闭 | 启用 | 冲突 | 新增课表条目时检测是否与已有课程冲突 |
| 冲突时行为 | 仅提醒 / 提醒并阻止 | 仅提醒 | 冲突 | 冲突时的处理方式 |
| 抢单时冲突处理 | 先抢后提醒 / 先检测再抢 | 先抢后提醒 | 冲突 | 抢单时是否先检测时间冲突 |
| 每周重复课程 | 启用 / 关闭 | 关闭 | 重复 | 支持每周重复的课程 |
| 自动生成未来周 | 启用 / 关闭 | 启用 | 重复 | 每周自动生成后续周的副本 |
| 提前生成周数 | 1-12周 | 4周 | 重复 | 一次性提前生成未来多少周 |
| 重复截止方式 | 按日期 / 按次数 / 无限期 | 按日期 | 重复 | 重复课程的截止条件 |
| 收入统计 | 启用 / 关闭 | 关闭 | 收入 | 在课表页显示收入统计 |
| 默认课时费 | 数字（元） | 100 | 收入 | 抢单自动入课表时使用的默认单价 |
| 今日提醒 | 启用 / 关闭 | 关闭 | 提醒 | 开机/每天早上弹窗显示今日课程 |
| 课前提醒 | 启用 / 关闭 | 启用 | 提醒 | 课前N分钟弹窗提醒 |
| 提前提醒时间 | 5/10/15/30分钟 | 15分钟 | 提醒 | 课前多久提醒 |
| 课表导出格式 | 文本 / CSV | 文本 | 导出 | 导出课表的格式 |

### 匹配规则（完整版）

`
消息到达
  │
  ├── 反向过滤：含“取消/暂停/停止/不需要/不要/没有”等否定词
  │     └── 跳过关键词层，直接进入AI判断
  │
  ├── 第一层：快速关键词匹配
  │     ├── 命中“词汇/阅读/语法/完型/听口/写作/抗遗忘”之一
  │     │     └── 类型 = 命中的关键词
  │     │
  │     └── 未命中 → 进入第二层
  │
  ├── 第二层：AI智能识别（需启用）
  │     ├── AI判定为接单
  │     │     ├── AI提取到类型 → 类型 = AI提取的类型
  │     │     └── AI未提取到类型 → 类型 = 默认值（词汇）
  │     │
  │     └── AI判定非接单 → ❌ 忽略
  │
  └── 第三层：模式过滤
        ├── 多项模式 → ✅ 抢单（不限类型）
        │
        └── 单项模式
              ├── 类型匹配勾选 → ✅ 抢单
              └── 类型不匹配 → ❌ 忽略
`

**课表冲突检查（在抢单成功后触发）：**

`
抢单成功
  ├── 自动入课表已启用 → 检测时间冲突
  │     ├── 无冲突 → 自动加入课表
  │     └── 有冲突 → 按用户配置（仅提醒 / 阻止）
  │
  └── 自动入课表未启用 → 仅记录抢单，不入课表
`

### 应用场景

- **家教中介群抢单**：群里发布学生需求信息，系统自动识别并抢单回复
- **英语机构群接单**：词汇/阅读/语法/听口/写作等不同科目的辅导需求
- **代课教练群抢单**：消息不含具体科目但明显是教学需求（“寻代课教练”“正课教练”等）
- **抢时间敏感的单**：热门学生需求，谁先回复谁接单，机器人0延迟秒回
- **多人分工**：不同人使用不同的配置，如A只接“词汇+阅读”，B只接“听口+写作”

### 课表应用场景（新增）

- **日常上课提醒**：每天早上看一眼课表就知道今天要上什么课
- **冲突避免**：已经接了周一的课，不会再不小心抢到同一时间段的单
- **收入统计**：未来可以根据课表统计每周/每月上课节数和收入
- **学生管理**：每个学生叫什么、上什么课、什么时间，一目了然

---

## 商业模式

### 售卖方案

| 项目 | 内容 |
|------|------|
| **收费方式** | 按月订阅：29元/月 |
| **授权方式** | 绑定微信号——程序启动时检测当前登录的微信账号，在服务器端验证授权 |
| **试用期** | 3天免费试用（仅商业版支持） |
| **支付方式** | 支付宝/微信转账，手动发放授权码 |

### 授权验证流程

`
用户输入授权码
  │
  ├── 本地生成机器码（硬盘序列号+网卡MAC的Hash）
  ├── 读取当前登录微信号
  ├── 发送到授权服务器
  │     ├── 授权码有效 → 服务器记录「微信号+机器码+授权到期日」
  │     │               → 本地保存授权凭证（加密文件）
  │     │               → 解锁全部功能
  │     │
  │     └── 授权码无效/已过期 → 提示激活失败/续费
  │
  └── 每次启动时联网校验一次（离线缓存7天，超过7天需联网验证）
`

### 防盗版设计

- **双重绑定**：微信号 + 机器码 → 换机器或换号都需要重新激活
- **授权文件加密**：本地授权凭证AES加密，无法伪造
- **定期在线校验**：每7天要求联网验证一次
- **代码混淆**：Python打包时使用混淆工具，增加逆向难度

### 定价理由

29元/月是基于以下逻辑：
- 比一杯奶茶贵一点，用户决策成本低
- 如果能靠抢单接到课，一节课收入远超29元
- 按月订阅可持续收入，不用反复找新客户

---

## AI 系统提示词

`
[角色]
    你是微信抢单系统的智能识别引擎。
    
    你的核心职责是判断微信群消息是否为“家教/辅导/培训类接单信息”。
    你理解中文自然语言的各种不规范表达，能从“寻代课教练”“正课老师”“准高一女生需有耐心”等
    口语化陈述中准确识别出接单意图。

[任务]
    分析给定的微信群消息，判断是否属于接单信息。如果是，提取其中的科目类型。
    
    输出JSON格式的判断结果。

[技能]
    - **意图识别**：准确区分“这是接单消息” vs “这不是接单消息”
    - **类型提取**：从消息中提取涉及的具体科目类型（词汇/阅读/语法/完型/听口/写作/抗遗忘）
    - **否定识别**：识别“取消/暂停/不需要/不要/没有”等否定表达，有否定词时跳过关键词层
    - **默认兜底**：判定为接单但未提取到类型时，默认输出“词汇”

    `
    判断以下微信群消息是否属于家教/辅导/培训接单类型的信息。
    
    输出JSON格式：
    {
      is_order: true/false,
      confidence: 0.95,
      types: [词汇, 阅读],
    }
    `

[总体规则]
    - 宁抢勿漏：有疑虑时倾向于判断为接单信息
    - 每条消息独立判断，不依赖上下文
    - 输出必须为有效JSON格式
    - 科目类型仅限：词汇、阅读、语法、完型、听口、写作、抗遗忘
    - 判定为接单但未提取到类型时，default_type设为true，由系统自动填充默认类型

[工作流程]
    [消息判断阶段]
        目的：判断是否抢单
        
        第一步：接收消息原文
        
        第二步：检查否定词
            - 含取消/暂停/停止/不需要/不要/没有 → 跳过关键词层，直接进入AI判断
            - 否则正常进入第三步
        
        第三步：快速关键词检查
            - 如果命中词汇/阅读/语法/完型/听口/写作/抗遗忘
              → 直接判断为接单，类型=命中的关键词
            - 否则进入第四步
        
        第四步：AI智能判断（如已启用）
            调用 [消息意图判断]
            - is_order=true → 触发抢单
            - types为空且default_type=true → 使用默认词汇
            - is_order=false → 忽略
        
        第五步：模式过滤
            - 多项模式 → 直接抢
            - 单项模式 → 检查类型是否匹配用户勾选

`

---

## 两套完整技术方案

---

## 方案A：Python UI 自动化方案（推荐给追求稳定、低风险的用户）

### 核心理念
不需要任何非官方协议，通过Windows UI自动化框架模拟人的操作。微信电脑版正常登录，脚本在后台监听窗口内容、模拟键盘鼠标操作。

### 技术栈

| 组件 | 技术 | 用途 |
|-----|------|------|
| 语言 | Python 3.10+ | 主程序语言 |
| UI自动化 | uiautomation | 监听微信聊天列表、读取消息、模拟点击 |
| AI判断 | OpenAI API (GPT-4o-mini) | 智能识别消息是否为接单信息 |
| 配置页面 | Flask + 原生HTML/CSS/JS | 本地Web配置界面 |
| 持久化 | JSON文件 | 保存配置和抢单记录 |
| 系统托盘 | pystray | 后台托盘图标 |
| 开机自启 | Windows 任务计划程序 | 开机自动运行 |
| 打包工具 | PyInstaller / Inno Setup | 打成一键安装包（商业版） |
| 授权系统 | Flask 服务端 + AES 加密 | 在线授权校验（商业版） |

### 系统架构图

`
┌─────────────────────────────────────────────────┐
│              你的Windows电脑                       │
│                                                    │
│  ┌──────────────────────┐    ┌─────────────────┐  │
│  │  微信电脑版           │    │  Python脚本       │  │
│  │                      │◄──►│                  │  │
│  │  群聊窗口            │    │  1. uiautomation │  │
│  │  聊天记录区域         │    │     监听消息      │  │
│  │                      │    │  2. 关键词匹配     │  │
│  └──────────────────────┘    │  3. AI智能判断     │  │
│                               │  4. 自动回复      │  │
│  ┌──────────────────────┐    │  5. 记录日志      │  │
│  │  Web配置页面           │◄──►│                  │  │
│  │  (http://localhost)   │    │  6. 托盘图标      │  │
│  └──────────────────────┘    └──────┬──────────┘  │
│                                      │             │
│                               ┌──────▼──────────┐  │
│                               │  LLM API         │  │
│                               │  (AI智能识别)     │  │
│                               └─────────────────┘  │
└─────────────────────────────────────────────────┘
`

### 工作原理（极简说明）

1. uiautomation 找到微信窗口 → 定位到聊天列表区域
2. 定期扫描聊天列表，获取最新消息的文本内容
3. 对每条新消息执行完整匹配规则（否定词 → 关键词 → AI → 模式过滤）
4. 匹配成功 → 定位到该消息 → 模拟右键 → 点击引用回复 → 输入配置的回复内容 → 发送
5. 整个过程模拟人手操作，微信感知不到任何异常

### 优点
- ✅ 封号风险极低（纯模拟人操作，不改微信协议）
- ✅ 开发成本低（全Python，依赖成熟开源库）
- ✅ 部署简单（下载解压双击运行）
- ✅ 配置页面直观好用
- ✅ 可扩展（加功能、改回复内容很方便）
- ✅ 免费或极低成本（AI接口可选免费版或不用）

### 缺点
- ❌ 微信窗口不能最小化到托盘（需在后台显示，可置为不遮挡的小窗口）
- ❌ 对微信更新可能产生兼容性问题（极少发生）
- ❌ 响应速度受限于UI扫描频率（通常0.5-1秒，足够快于人手）
- ❌ 占用一个Windows用户会话

### 适用人群
- **完全不懂技术的人（首选方案）**
- 对封号零容忍的人
- 只有一台普通Windows电脑
- 愿意接受0.5-1秒的响应延迟

---

## 方案B：微信Hook协议方案

### 核心理念
使用开源Hook库（如WeChatMsg、ComWeChatRobot），通过DLL注入获取微信实时消息事件。

### 技术栈

| 组件 | 技术 | 用途 |
|-----|------|------|
| 语言 | Python 3.10+ | 主程序 |
| Hook库 | WeChatMsg / ComWeChatRobot | 注入微信进程获取消息事件 |
| AI判断 | OpenAI API | 同方案A |
| 配置页面 | Flask + 前端 | 同方案A |
| 持久化 | SQLite | 记录存储 |
| 系统托盘 | pystray | 同方案A |

### 对比总表

| 维度 | 方案A（UI自动化）✅推荐 | 方案B（Hook注入） |
|------|-----------------|-----------------|
| **封号风险** | 🟢 极低 | 🟡 中等 |
| **响应速度** | 🟡 0.5-1秒（足够快） | 🟢 毫秒级 |
| **部署难度** | 🟢 极简（双击运行） | 🔴 较复杂 |
| **微信窗口** | 需保持打开 | 可隐藏 |

---

## 产品路线图

### 第一阶段：自用版（预计1-2周开发）

**目标**：自己先用起来，跑通全流程

| 步骤 | 内容 | 里程碑 |
|------|------|-------|
| 1 | 搭建Python基础框架，实现微信窗口扫描 | ✅ 能读到群消息 |
| 2 | 实现关键词匹配 + 自动引用回复 | ✅ 能抢单 |
| 3 | 实现单项/多项模式 + 配置界面 | ✅ 能切换配置 |
| 4 | 接入AI智能识别（OpenAI API） | ✅ 能识别无关键词的接单 |
| 5 | 加入抢单记录 + 托盘通知 | ✅ 能看抢单记录 |
| 6 | 真实环境测试（跑3-5个群） | ✅ 稳定运行 |

**测试标准**：连续运行7天不崩溃、不漏消息、封号风险确认

### 第二阶段：商业化准备（预计1周）

| 步骤 | 内容 | 里程碑 |
|------|------|-------|
| 1 | 搭建授权服务器（轻量Flask服务） | ✅ 能生成/校验授权码 |
| 2 | 实现微信号绑定 + 机器码绑定 | ✅ 防复制 |
| 3 | 实现激活/续费流程 | ✅ 用户能自助激活 |
| 4 | 打包为一键安装包（Inno Setup） | ✅ 用户双击安装 |
| 5 | 压测授权系统安全性 | ✅ 抗破解 |

### 第三阶段：对外售卖

| 步骤 | 内容 |
|------|------|
| 1 | 定好价格（29元/月），开放购买 |
| 2 | 第一批卖给认识的家教群主/中介 |
| 3 | 收集反馈，修复问题 |
| 4 | 持续迭代+维护 |

---

## 技术风险说明

| 风险项 | 方案A | 方案B | 应对措施 |
|-------|------|------|---------|
| 微信版本升级 | 极少影响 | 可能失效 | 方案A几乎不受影响 |
| 被封号 | 几乎不可能 | 有可能 | 方案A可放心使用 |
| 漏接消息 | 低概率 | 不漏 | 方案A可缩短扫描间隔 |
| 电脑休眠/关机 | 停止工作 | 停止工作 | 取消电脑休眠，配置开机自启 |
| 商业版被破解 | 代码混淆可延缓 | 同左 | 双重绑定+在线校验 |


---

## 2026-07-13 课程反馈与手机提醒迭代

### 本次范围

- 本轮不修改、不扩展抢课/抢单功能。
- 增加“课程反馈”模块，支持主课反馈和抗遗忘反馈。
- 增加免费的手机系统日历提醒。

### 反馈业务规则

1. 主课按“用户 + 课程 + 上课日期”生成反馈，同一节周课在不同日期可分别生成。
2. 抗遗忘按“用户 + 学生 + 日期”强制合并。同一学生同一天即使有多次抗遗忘，也只保存一份综合反馈。
3. 不同学生的反馈不得合并。
4. 服务端会重新查询当天该学生的全部抗遗忘课程，不信任前端传入的部分课程 ID。
5. 反馈包含：上课内容、专注度、掌握度、存在问题、课后任务、下次计划、教师备注和最终正文。

### 生成方式

- 免费模板为默认能力，不需要 API Key，不产生额外费用。
- AI 只作为可选润色，使用当前用户自己的 AI 配置；未配置或请求失败时自动回退免费模板。
- AI 不得编造学生表现，只能润色用户明确填写的事实。

### 手机提醒

- 可下载单节课或当天全部课程的 ICS 日历文件。
- 每节课包含课前 30 分钟和 10 分钟两次系统日历提醒。
- 主课和抗遗忘都可单独加入日历；多次抗遗忘虽然合并为一份反馈，仍保留每次复习的日历提醒。

### 数据与权限

- 反馈记录、课程候选和日历导出全部以 `user_id` 过滤。
- 用户不能查看、更新或导出其他用户的课程和反馈。

### 验收标准

- 同一学生同一天 2 次或更多抗遗忘，页面只展示一个综合反馈项，数据库也只有一条对应记录。
- 重复生成会更新原记录，不会新增重复记录。
- 没有 AI Key 也能正常生成、编辑、保存和复制反馈。
- 手机导入 ICS 后，能看到正确时间和两次课前提醒。

### Changelog

- 2026-07-13：新增用户隔离的课程反馈数据表和 API。
- 2026-07-13：新增主课/抗遗忘反馈页面，实现同一学生同日抗遗忘只生成一份反馈。
- 2026-07-13：新增免费模板和可选 AI 润色回退机制。
- 2026-07-13：新增单节课/当日课程 ICS 导出与双提醒。


---

## 2026-07-30 登录恢复与邀请码修复

### 变更范围

- Railway 管理员忘记密码时，可临时设置 `ADMIN_RECOVERY_PASSWORD`，在服务启动时恢复 `ADMIN_PHONE` 对应账号。
- 恢复过程保留所有用户、课程、反馈和配置数据，只注销管理员的旧登录令牌。
- 相同恢复密码只执行一次；登录成功后必须删除 Railway 中的 `ADMIN_RECOVERY_PASSWORD`。
- 注册邀请码自动去除前后空格，错误提示明确要求新人获取最新邀请码。
- 新邀请码仍通过 Railway 的 `REGISTRATION_INVITE_CODE` 管理，修改后重新部署即可生效。

### 验收标准

- 管理员旧密码失效，新密码可以登录，其他用户密码与数据不受影响。
- 重启服务不会重复覆盖管理员后来修改的密码。
- 邀请码前后误带空格时仍可正常注册；旧邀请码在更新后立即失效。

### Changelog

- 2026-07-30：新增一次性管理员密码恢复变量，修复邀请码空格导致的新人注册失败。

- 2026-07-30：一次性重置码支持自动大写并忽略空格、连字符和展示文字，避免复制格式导致校验失败。

- 2026-08-06：注册邀请码迁移到仅服务端可读的持久化系统设置，管理员可在页面一键生成并复制新邀请码。
- 2026-08-06：密码恢复改为 15 分钟内有效的一次性重置链接，自动填充手机号与重置码。
- 2026-08-06：邀请码和管理员恢复指纹不再通过普通用户配置 API 返回。
---

## 2026-08-06 P0 稳定性整改

### 已实施

1. 移动端课表在 1060px 以下使用 minmax(0, 1fr)，卡片和内容区允许收缩，800px 周表只在自己的滚动容器内横向滚动，不允许撑大页面。
2. 新增和编辑课程在冲突确认后必须保持原有 HTTP 方法与 URL；编辑只能 PUT 原课程，不得 POST 生成副本。
3. DELETE /api/schedule/:id 在同一 SQLite 事务中删除主课和关联抗遗忘记录；任一步失败必须全部回滚。
4. 介绍页的“加载演示数据”进入免登录只读演示模式；演示数据不写入数据库，修改操作引导用户注册。
5. 课程提醒默认 30 秒轮询；页面不可见时暂停，禁止并发请求，失败后指数退避，最长 5 分钟。

### 验收标准

- 390px 视口下 documentElement.scrollWidth 不得大于 clientWidth；周表保留内部横向滚动。
- 编辑冲突并确认后，课程总数不变，原 ID 内容被更新。
- 删除主课后关联复习一并删除；模拟主课删除失败时关联复习不得丢失。
- 演示模式不发起用户课表写请求。
- 课程提醒不再存在 5 秒固定 setInterval。


---

## 2026-08-06 P1 核心体验整改

### 已实施

1. 空账号不再展示整张空周表，改为“添加第一节课 / 体验示例课表 / CSV 批量导入”新手引导。
2. 课程表单明确区分“单次课程”和“每周重复”，单次课必须有日期，周课必须有结束日期或次数；对时长、课时费、重复次数和复习间隔同时做前后端校验。
3. 课程和导出弹窗补充 dialog 语义、打开聚焦、Escape 关闭、焦点锁定和关闭后焦点返还。
4. 导出分为复制文本、PNG、打印 PDF、CSV 和 ICS；本周无课时禁用导出。
5. 课表和反馈页在手机端改为折叠导航，主要触控区按 44px 设计；周表打开后滚动到当前时间附近。
6. URL 信息架构统一为 `/` 介绍页、`/schedule` 课表、`/feedback` 课程反馈、`/automation` 微信自动化、`/admin` 管理员后台。
7. 管理员邀请码和一次性重置码迁移到独立 `/admin` 页，仍由服务端校验管理员身份。
8. 配置自动保存显示“保存中 / 已保存 / 保存失败”；失败时回滚控件并支持点击重试。
9. 配置开关不再 `display:none` 隐藏复选框，保留键盘焦点并自动补充可读标签。
10. 微信自动化页明确“Railway 云端 + Windows 本地助手”边界，显示助手、微信、OCR 和心跳状态；本地助手离线时禁用抢单和测试回复。

### 验收标准

- 390px 课表、反馈、配置和管理员页的 `scrollWidth` 不得超过 `clientWidth`。
- 空账号只显示引导，新增第一节课后恢复完整课表。
- 课程表单初始焦点在学生输入框，默认时间为下一个半小时，课时费来自用户配置。
- 五种导出入口可用，导出文本和文件不包含其他用户数据。
- 配置保存失败时页面值与服务器值不得假装成功。
- 云端无本地助手时，开始抢单和测试回复必须不可用。

---

## 2026-08-06 P2 安全与细节整改

### 已实施

1. 周课表工作时段改为用户可配置，默认 07:00～23:30，开始和结束时间按账号隔离保存，周表及自动滚动共同使用该范围。
2. 课程块和日历日期继续使用可聚焦的原生按钮；删除课程、清除抗遗忘和冲突继续操作统一使用站内对话框，支持 Escape、焦点锁定和焦点返还。
3. 抗遗忘生成前调用服务端预览接口，展示生成数量、具体日期和每个日期的课程冲突；存在冲突时必须再次确认，生成接口也会重新校验避免绕过。
4. 微信开始、停止、暂停、恢复、测试回复、自动扫描群和诊断接口改为管理员鉴权；普通用户界面隐藏这些本机服务操作，只保留个人配置和群组数据管理。
5. 所有页面增加本地 SVG favicon，不依赖境外资源或付费服务。
6. 所有响应增加 CSP、HSTS（HTTPS 时）、X-Content-Type-Options、Referrer-Policy、X-Frame-Options 和 Permissions-Policy；继续保留 HTTPS 与 HttpOnly、SameSite 会话 Cookie。

### 验收标准

- 两个账号保存不同工作时段时互不影响，刷新后恢复各自设置。
- 删除课程和清除复习不再调用原生 confirm()，取消操作不会发送删除请求。
- 抗遗忘预览只检查当前用户课程，返回生成日期、数量和冲突课程；生成时冲突未经确认返回 409。
- 普通用户直接调用本机微信控制接口统一返回 403，管理员身份仍由服务端判断。
- favicon 可直接访问，HTML 页面均引用该图标。
- HTTPS 响应包含 HSTS，所有响应包含 CSP、nosniff 和 Referrer-Policy。

---

## 2026-08-07 Beta 上线前优化

### 批量导入

1. CSV 导入流程固定为“选择文件 → 校验表头 → 全量解析 → 逐行预览 → 标记错误和冲突 → 用户确认 → 单次事务提交”。
2. 必填列为：学生姓名、课程类型、上课日期、开始时间、时长(分钟)、课时费；状态和备注可选。
3. 日期使用 `YYYY-MM-DD`，时间使用 `HH:MM`；单次最多 200 条，前端文件限制 512KB，服务端请求限制 2MB。
4. 服务端接口 `POST /api/schedule/import` 同时支持预览和提交；提交使用 UUID `importId` 和内容哈希保证请求重试及重复文件不会重复创建。
5. 任意写入失败时整批回滚，不允许出现半成功；错误必须返回 CSV 原始行号。
6. `DELETE /api/schedule/import/latest` 只撤销当前用户最近一次完成的导入，不影响手动课程或其他用户数据。
7. 页面必须提供可下载的正式 CSV 模板和一行示例数据。

### 演示模式

1. 演示课程的“添加到手机日历”由浏览器直接生成 ICS，不再请求负数课程 ID。
2. 已登录用户的演示 CTA 显示“返回我的课表”，未登录用户显示“免费注册并创建课表”。
3. 演示模式隐藏删除、状态修改和抗遗忘写操作；编辑改为“查看课程说明”。
4. 四条示例数据分别说明待上课提醒、课后反馈、手机日历和收入/抗遗忘能力。

### 移动端

1. 680px 以下默认显示按日期分组的课程列表，只展示有课程的日期和时间；用户可切换到周表。
2. 列表自动标记并定位到下一节课；周表仍保留独立滚动和用户自定义时段。
3. 移动端按钮和菜单触控高度不低于 44px；编辑、删除、状态和日历操作收进“更多”。

### 导出与内容安全

1. CSV 单元格对 `= + - @` 等公式前缀进行中和，并使用可再次导入的统一表头。
2. ICS 使用 UTC 时间、稳定 UID、DTSTAMP、RFC 5545 文本转义和 CRLF 换行，并保留课前 30 分钟及 10 分钟提醒。
3. PNG 根据换行后的内容动态计算高度，长姓名和备注不得被画布截断。
4. 页面只保留一个“复制本周课表”入口；导出弹窗提供 PNG、打印/PDF、CSV 和 ICS。
5. 功能文案统一使用“添加到手机日历”，不再把 ICS 下载称为手机推送。

### SEO 与公开页面

1. `/schedule`、`/feedback`、`/automation`、`/admin`、`/login` 使用 `noindex,nofollow`。
2. 介绍页提供独立标题、描述、canonical、Open Graph、FAQ、隐私政策、用户协议和明确 CTA。
3. 介绍页不设置 noindex，作为唯一需要搜索引擎收录的产品页面。

### 验收标准

- 演示模式点击日历操作不产生 `/api/schedule/-1/calendar.ics` 请求和 404。
- CSV 缺列或错误行不允许提交，错误显示准确行号；第二行写入失败时第一行也必须回滚。
- 同一 `importId` 重试或同一文件再次上传不新增重复课程。
- 撤销最近导入后仅对应批次课程消失，其他用户和手动课程保持不变。
- 390px 下默认列表视图、页面无横向溢出，主要触控区域不低于 44px。
- 导出的 ICS 包含 UID、DTSTAMP、UTC DTSTART/DTEND、CRLF 和正确转义。
