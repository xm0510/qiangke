# 课表系统 · 详细设计

## 1. 数据模型（完整版）

每个课表条目对应一节课，存储在 SQLite 数据库中。

```
表名：schedule_entries

字段                | 类型     | 说明
-------------------|----------|----------------------------------------------
id                 | INTEGER  | 主键，自增
student_name       | TEXT     | 学生姓名/称呼（如张三、新初一男生）
subject_type       | TEXT     | 科目类型：词汇/阅读/语法/完型/听口/写作/抗遗忘
day_of_week        | INTEGER  | 星期几（1=周一，2=周二 ... 7=周日）
start_time         | TEXT     | 开始时间 HH:mm（如 20:00）
duration_min       | INTEGER  | 时长（分钟），如 60
status             | TEXT     | pending/confirmed/cancelled/completed
notes              | TEXT     | 备注（如基础中等，需有耐心）
source             | TEXT     | grab（抢单）/ manual（手动）
source_grab_id     | INTEGER  | 关联的抢单记录ID
is_recurring       | INTEGER  | 是否重复课程 0=否 1=是 **新增**
recur_end_date     | TEXT     | 重复截止日期 YYYY-MM-DD **新增**
recur_until_count  | INTEGER  | 重复次数上限 **新增**
parent_entry_id    | INTEGER  | 父条目ID（重复生成的子条目关联到此）**新增**
price_per_session  | REAL     | 单节课时费（元），如 150 **新增**
created_at         | DATETIME |
updated_at         | DATETIME |
```

**新增字段说明**：
- is_recurring：标记为每周重复的课程，系统自动生成未来周的副本
- recur_end_date / recur_until_count：重复的截止条件，满足任一即停止
- parent_entry_id：所有自动生成的子条目指向同一个父条目，方便批量修改
- price_per_session：单节课时费，用于收入统计

## 2. 重复课程系统

### 什么情况下会触发重复？

接单消息或手动添加时，如果上课时间包含"每周""每""平时"等每周固定含义，AI 会标记为重复课程。

手动添加时，用户可以直接勾选"每周重复"开关。

### 配置选项

新增配置项（在课表管理板块）：

| 配置项 | 可选值 | 默认值 | 说明 |
|-------|-------|-------|------|
| 每周重复课程 | 启用 / 关闭 | 关闭 | 启用手动添加时可勾选每周重复 |
| 自动生成未来周 | 启用 / 关闭 | 启用 | 每周自动生成后续周的重复课程 |
| 提前生成周数 | 1-12周 | 4周 | 一次性提前生成未来多少周的副本 |
| 重复截止方式 | 按日期/按次数 | 按日期 | 默认按学期末或无限期 |
| 重复截止日期 | 日期选择 | 本学期末 | 重复到该日期为止 |

### 数据存储方式

```
方案：单条存+自动预生成

原理：
- 用户添加/抢到一条"每周三20:00"的课
- 系统保存为一条 is_recurring=1 的父条目
- 同时自动生成未来N周（默认4周）的子条目（parent_entry_id指向父条目）
- 每个子条目独立可编辑/取消/完成
- 每周定时任务：检查父条目，生成下一周的副本（如果未超出截止条件）

优点：
- 每个子条目独立状态（周三的课确认了，下周三的还待确认）
- 修改父条目（如改时间）可以同步到所有未发生的子条目
- 取消某周不影响其他周
- 离线可用（不需要网络）
```

### 配置页面 UI

```
┌─ 重复课程设置 ────────────────────────┐
│                                        │
│  ☑ 每周重复课程                        │
│  ☑ 自动生成未来周                       │
│     提前生成：[4周 ▼]                   │
│                                        │
│  重复截止方式：                         │
│  ○ 按日期：[2026-08-31 〓]               │
│  ● 按次数：[10次]                       │
│  ○ 无限期（不截止）                    │
│                                        │
└────────────────────────────────────────┘
```

### 手动添加时的表单（含重复选项）

```
┌─ 添加课程 ──────────────────────────┐
│                                      │
│  学生姓名/称呼                        │
│  [________________________]           │
│                                      │
│  科目类型                             │
│  [词汇 ▼]                             │
│                                      │
│  上课时间                             │
│  星期：[周一 ▼]  时间：[20:00]        │
│  时长：[60分钟]                       │
│                                      │
│  ☑ 每周重复（推荐）                   │
│     └─ 截止：[2026-08-31] 或 [10次]   │
│                                      │
│  课时费：[150] 元/节 （用于收入统计）  │
│                                      │
│  状态                                 │
│  ● 已确认  ○ 待确认                   │
│                                      │
│  备注                                 │
│  [基础中等，需有耐心___________]      │
│                                      │
│  [保存]  [取消]                       │
└──────────────────────────────────────┘
```

### 课表中的重复课程显示

- 重复课程行尾显示 🔄 图标
- 悬停提示："每周重复，已生成至 2026-08-31"
- 单击可编辑当前周的副本（不改其他周）
- 右键菜单："编辑整组"（改父条目，同步所有未发生副本）

## 3. 收入统计系统

### 配置选项

新增配置项（在课表管理板块）：

| 配置项 | 可选值 | 默认值 | 说明 |
|-------|-------|-------|------|
| 收入统计 | 启用 / 关闭 | 关闭 | 启用后在课表页显示收入 |
| 默认课时费 | 数字 | 100 | 抢单自动入课表时使用的默认单价 |
| 收入统计范围 | 本周/本月/全部 | 本周 | 默认显示的统计周期 |

### 数据显示位置

课表页面顶部增加收入统计栏：

```
┌──────────────────────────────────────────────────┐
│  课表管理 ｜ 本周 2026-07-06 ~ 2026-07-12        │
│  [← 上一周] [回到今天] [下一周 →]               │
├──────────────────────────────────────────────────┤
│  [课表内容...]                                    │
├──────────────────────────────────────────────────┤
│  课程总数: 4节                                   │
│  本周收入: ￥600 ｜ 本月收入: ￥2,400           │
│  已确认: 3节 ￥450 ｜ 待确认: 1节 ￥150         │
└──────────────────────────────────────────────────┘
```

### 收入详情弹窗

点击收入数字可展开详细统计：

```
┌─ 收入明细 ───────────────────────────┐
│                                       │
│  本周（07-06 ~ 07-12）                │
│  ──────────────────                  │
│  词汇: 2节 x 150 = ￥300              │
│  阅读: 1节 x 200 = ￥200              │
│  语法: 1节 x 150 = ￥150              │
│  ──────────────────                  │
│  小计: 4节 = ￥650                    │
│                                       │
│  本月累计: ￥2,400                     │
│  本年累计: ￥15,600                    │
│                                       │
│  [关闭]                               │
└───────────────────────────────────────┘
```

### 自动计算规则

- 每个课表条目有独立的 price_per_session 字段
- 抢单自动入课表时使用默认课时费（可在配置中修改）
- 手动添加时直接填写课时费
- 未填写课时费的条目按 ￥0 计算（不纳入收入统计）
- 只有 status=confirmed（已确认）和 completed（已完成）的才计入收入
- pending（待确认）的课时费显示为预估收入，灰色标注
- cancelled（已取消）的不计入任何收入

### 冲突弹窗 UI

```
+--- 冲突提醒 -------+
|                      |
|  新课程与已有冲突！   |
|                      |
|  已有：周三20:00     |
|  李四.阅读 [已确认]  |
|                      |
|  新课：周三20:00     |
|  王五.语法 [待确认]  |
|                      |
|  [仍然加入] [取消]   |
+----------------------+
```

## 5. 状态流转

```
抢单成功/手动添加 --> 待确认 --> 已确认 --> 已完成
                              |          |
                              --> 已取消
```

状态变更操作：
- 在课表页面点击状态标签即可切换
- 已取消的灰色显示，仍可查看
- 已完成自动归档到历史课程
- 收入统计仅计算已确认和已完成的课程

## 6. 今日课表提醒

| 配置项 | 可选值 | 默认值 | 说明 |
|-------|-------|-------|------|
| 今日提醒 | 启用/关闭 | 关闭 | 开机/早上弹窗 |
| 课前提醒 | 启用/关闭 | 启用 | 课前X分钟提醒 |
| 提前时间 | 5/10/15/30分钟 | 15分钟 | |

触发时机：开机后、早上8:00、课前N分钟

## 7. 导出课表

文本格式和CSV格式，含课程、状态、课时费信息。

## 8. 完整联动流程

见Product-Spec.md中的流程图。

## 9. 配置项一览

| 配置项 | 所属功能 | 默认值 |
|-------|--------|-------|
| 自动入课表 | 基础 | 关闭 |
| 冲突检测 | 冲突 | 启用 |
| 冲突时行为 | 冲突 | 仅提醒 |
| 抢单时冲突处理 | 冲突 | 先抢后提醒 |
| 每周重复 | 重复 | 关闭 |
| 自动生成未来周 | 重复 | 启用 |
| 收入统计 | 收入 | 关闭 |
| 默认课时费 | 收入 | 100 |
| 今日提醒 | 提醒 | 关闭 |
| 课前提醒 | 提醒 | 启用 |

